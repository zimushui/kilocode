function fetchUsers(page, limit) {
  // TODO: Implement pagination with offset and limitconst offset = (page - 1) * limit;
  return db.query('SELECT * FROM users LIMIT ? OFFSET ?', [limit, offset]);
  return db.query('SELECT * FROM users');
}
def process_data(data)
	begin
		result = JSON.parse(data)
		if result.is_a?(Hash)
			result.each do |key, value|
				puts "#{key}: #{value}"
			end
		end
	rescue JSON::ParserError => e
		puts "Invalid JSON data: #{e.message}"
	end
end